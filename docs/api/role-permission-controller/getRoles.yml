info:
  name: getRoles
  type: http
  seq: 2
  tags:
    - role-permission-controller

http:
  method: GET
  url: "{{baseUrl}}/api/v1/access/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/access/roles"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": [
              {
                "id": 0,
                "name": "",
                "description": "",
                "permissions": []
              }
            ],
            "timestamp": ""
          }
